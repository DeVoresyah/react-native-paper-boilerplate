import { connect } from '../../styles/osmiProvider';

export default connect({
  textInput: 'border border-dark-500 p-2 rounded-md',
  button: 'bg-danger-500 py-4 px-3 rounded-md mt-2',
  buttonText: 'text-white text-center'
})
